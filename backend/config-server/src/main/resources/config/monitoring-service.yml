spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/monitoring_service?createDatabaseIfNotExist=true
    username: root
    password: testUser
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
server:
  port: 8080